// O código  vai concatenar os elementos do array, ou seja, os elementos se tornará uma unica frase, com o uso do "JOIN" 

let nomes = ["Paulo", "Guilherme", "Matias"]
let nomeconcat = nomes.join(" ")
console.log(nomeconcat.toUpperCase())